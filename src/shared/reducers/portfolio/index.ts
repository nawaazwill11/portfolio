import { Map } from 'immutable'
import { Action } from 'redux'


const portfolioReducer = (state = {}, actions: Action) => {
    switch(actions.type) {
        default: return state
    }
}

export default portfolioReducer